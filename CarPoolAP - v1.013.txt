
Generated by Back-end API - Portal coding assistant

New Changes against v1.012

What this patch includes (in your lib/)

Removes cross-feature presentation coupling: replaces non-auth feature imports of AuthProvider with a new SessionProvider in core/session/.

Standardizes ride_search usecases into application/usecases/ (moved from domain/usecases/ and updated imports).

Implements missing auth usecases under features/auth/application/usecases/ and refactors AuthProvider to use them.

Replaces exception-string error handling with typed Failures in key providers (Auth, RideSearch, Booking, MyRides, PostRidePublish).

Removes time/formatting logic from MyRide domain entity and moves it into a presentation extension.

Introduces an auth credential + strategy/facade model so adding LinkedIn/WhatsApp won’t require rewriting Provider/UI APIs (you add a new Credential + Strategy and register it in DI).