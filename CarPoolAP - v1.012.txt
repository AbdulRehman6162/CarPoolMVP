
Generated by Back-end API - Portal coding assistant

New Changes against v1.011

Adding of Supabase Auth from real DBs

What these patches do
1) Adds Supabase remote datasource (data layer only)

New file:

lib/features/auth/data/datasources/supabase_auth_remote_data_source.dart

This implements your existing AuthRemoteDataSource using Supabase:

login → signInWithPassword

signup → signUp (stores name in metadata)

verifyOtp → verifyOTP (tries signup then falls back to magiclink by enum-name to avoid SDK variance)

authStateChanges → maps Supabase onAuthStateChange into AuthUserModel?

logout → signOut

2) Extends your AuthRemoteDataSource (still inside data layer)

Your domain contracts remain unchanged. This change is internal to the data layer:

Adds Stream<AuthUserModel?> get authStateChanges;

Adds Future<void> logout();

Mock remote is updated accordingly.

3) Improves repository robustness (still Clean)

AuthRepositoryImpl now:

subscribes to remote.authStateChanges

keeps local cache + domain stream consistent if session changes

calls remote.logout() on logout

cancels remote subscription in dispose()

4) DI switches to Supabase (with safe fallback)

lib/di/app_di.dart now tries:

SupabaseAuthRemoteDataSource(Supabase.instance.client)

If Supabase isn’t initialized (e.g., tests), it falls back to:

MockAuthRemoteDataSource()

So your app/tests won’t crash if you haven’t initialized Supabase yet.


