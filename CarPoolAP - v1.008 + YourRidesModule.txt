Generated by Back-end API - Portal coding assistant

New Changes against v1.007

+ YourRidesModule Inject


✅ User flow coverage

Logged-in user can see All rides / Upcoming / Completed (tabs)

All rides is sorted latest first (descending by departure)

Each ride card shows whether user was Driver or Passenger (icon + chip)

Tap a ride → opens Ride Details

shows As driver or As passenger view automatically (based on ride.role)

Driver details view includes seat progress + booked passengers list + actions + CTA

Passenger details view includes driver info + passenger actions + CTA

Archived rides

from “Archived rides” button on main page

archived list shows archived rides (driver/passenger mixed)

archived details screen uses same details page with archived mode (actions disabled)

✅ Clean Architecture & SOLID in this module

Domain: MyRide, MyRideRole, MyRideStatus, RideUser, RidePassenger

Data layer: DTOs (MyRideModel, etc.) + mock datasource (MyRidesRemoteDataSource)

Repository abstraction: MyRidesRepository

Use cases: GetMyRidesUseCase, GetArchivedRidesUseCase, GetMyRideDetailsUseCase

Presentation: Provider (MyRidesProvider) + pages + widgets

DIP: Details page uses ChatLauncher (already in your core platform boundary)