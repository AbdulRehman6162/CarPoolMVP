
Generated by Back-end API - Portal coding assistant

New Changes against v1.013

1. Analysis: How this Diff Improves Your Code
A. New Features Added
Phone Authentication (SMS/WhatsApp): The diff adds the infrastructure to request and verify OTPs via Phone (E.164 standard).

Email Verification Gating: It introduces a "Challenge" system. If a user logs in but hasn't verified their email, the Router now intercepts them and forces them to the Verification page before they can access the app.

Password Management: Adds flows for Forgot Password and Update Password.

Biometric Abstraction: Adds an interface for Biometrics (FaceID/Fingerprint), enabling you to add this feature later without breaking the login logic.

B. Architecture & SOLID Design Improvements
Single Responsibility Principle (SRP):

Before: The Router just checked isLoggedIn.

After: The AuthChallenge logic separates the state of the user (Verified vs. Unverified) from the navigation logic. The AuthProvider manages the state, and the GoRouter manages the enforcement.

Open/Closed Principle (OCP):

The BiometricAuth interface is introduced. The diff provides a NoopBiometricAuth (No-operation) implementation. This allows you to "close" the auth logic to modification but "open" it to extension (e.g., adding LocalAuth later) by simply swapping the dependency injection.

Interface Segregation Principle (ISP):

The AuthRemoteDataSource is expanded to include specific methods for requestPhoneOtp, verifyPhoneOtp, etc., ensuring that data sources explicitly define what auth capabilities they support.

Security & Scalability:

Session Refresh: The _bootstrap method now calls refreshCurrentUser. This validates the local token against the server on app startup, ensuring a revoked user cannot access the app just because they have a cached token.