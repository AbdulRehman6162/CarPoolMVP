--- a/lib/app/router.dart
+++ b/lib/app/router.dart
@@ -59,7 +59,7 @@
       }

 

       // If logged in, keep user out of auth screens (except password update/verify-email if gated).

-      if (isLoggedIn && (isLogin || isSignup || isPhoneLogin || isForgotPassword || isAuthCallback)) {

+      if (isLoggedIn && (isLogin || isSignup || isPhoneLogin || isForgotPassword || isAuthCallback(state))) {

         return '/';

       }

 


--- a/lib/features/auth/domain/repositories/auth_repository.dart
+++ b/lib/features/auth/domain/repositories/auth_repository.dart
@@ -8,6 +8,12 @@
 

   /// Sign in using any supported credential (email/password, OAuth, OTP etc).

   Future<AuthUser> signIn(AuthCredential credential);

+

+  /// Starts OAuth sign-in for a provider (Google / Apple / etc) without awaiting completion.

+  ///

+  /// The session is typically completed via deep-link callback; listen to [authStateChanges].

+  Future<void> startOAuthSignIn(OAuthProvider provider);

+

 

   /// Start signup using credential (e.g. email/password signup).

   Future<void> signUp(AuthCredential credential);


--- a/lib/features/auth/data/repositories/auth_repository_impl.dart
+++ b/lib/features/auth/data/repositories/auth_repository_impl.dart
@@ -57,6 +57,12 @@
     _currentUser = cached?.toEntity();

     return _currentUser;

   }

+

+  @override

+  Future<void> startOAuthSignIn(OAuthProvider provider) async {

+    await _remote.startOAuthSignIn(provider: provider);

+  }

+

 

   @override

   Future<AuthUser> signIn(AuthCredential credential) async {

